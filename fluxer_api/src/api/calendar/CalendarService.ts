import { CalendarRepository } from "./CalendarRepository"`n`nexport class CalendarService {`n    private repository: CalendarRepository`n`n    constructor() {`n        this.repository = new CalendarRepository()`n    }`n`n    async createEvent(event: any, userId: string): Promise<void> {`n        await this.repository.saveEvent(event)`n    }`n`n    async editEvent(eventId: string, updatedEvent: any, userId: string): Promise<void> {`n        await this.repository.saveEvent({ ...updatedEvent, id: eventId })`n    }`n`n    async deleteEvent(eventId: string, userId: string): Promise<void> {`n        await this.repository.deleteEvent(eventId)`n    }`n`n    async getEvents(userId: string): Promise<any[]> {`n        return await this.repository.getAllEvents()`n    }`n}