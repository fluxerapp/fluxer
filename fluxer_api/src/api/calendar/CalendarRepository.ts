import { PostgresKvQueryExecutor } from "../database/PostgresKvQueryExecutor"`n`nexport class CalendarRepository {`n    private executor: PostgresKvQueryExecutor`n`n    constructor() {`n        this.executor = new PostgresKvQueryExecutor()`n    }`n`n    async saveEvent(event: any): Promise<void> {`n        await this.executor.insert("events", event)`n    }`n`n    async getEvent(eventId: string): Promise<any> {`n        return await this.executor.query("events", { id: eventId })`n    }`n`n    async deleteEvent(eventId: string): Promise<void> {`n        await this.executor.delete("events", { id: eventId })`n    }`n`n    async getAllEvents(): Promise<any[]> {`n        return await this.executor.query("events")`n    }`n}